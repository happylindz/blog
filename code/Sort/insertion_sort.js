function insertion_sort(arr) {
	if(arr.length <= 1) {
		return arr;
	}
	for(let i = 1; i < arr.length; i++) {
		let temp = arr[i];
		for(let j = i - 1; j >= 0; j--) {
			if(arr[j] > ) {

			}
		}
	}
} 